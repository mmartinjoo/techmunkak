[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dbt = [
    "dbt-core>=1.12.0",
    "dbt-postgres>=1.11.0",
]
dev = ["pytest", "ruff", "mypy"]

[tool.uv]
default-groups = ["dev", "dbt"]